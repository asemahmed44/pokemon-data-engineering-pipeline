# pokemon-data-engineering-pipeline
A complete Data Engineering ETL pipeline using Python, Pandas, SQLite, and SQL.  The project extracts data from PokéAPI, transforms it, loads it into a SQLite data warehouse,  builds a Star Schema (Fact &amp; Dimension tables), and runs analytical SQL queries.
